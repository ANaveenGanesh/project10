const http=require('http')
const {load}=require(''./cmr.js);
const ttp=require('ttp')
const app=express();
const port=3000;
app.listen(port,()+>console.log("server is running"));
app.get('/user',(req,res)=>{
    res.send('load'))
});
